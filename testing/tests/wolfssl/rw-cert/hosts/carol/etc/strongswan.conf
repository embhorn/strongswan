# /etc/strongswan.conf - strongSwan configuration file

swanctl {
  load = pkcs1 pem wolfssl x509 revocation constraints
}

charon-systemd {
  load = nonce pkcs1 pem wolfssl x509 revocation constraints curl kernel-netlink socket-default updown vici

  rsa_pss = yes
}
